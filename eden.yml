config:
    name: CLIP text to image
    author: abraham
  
build:
    - pip install ml4a --no-deps
    - pip install git+https://github.com/openai/CLIP.git --no-deps
    - pip install -r requirements.txt

run:
    - python3 server.py
    